import 'package:Worklyfe/core/widgets/text.dart';
import 'package:flutter/material.dart';

class WorklyfeScreen extends StatelessWidget {
  const WorklyfeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: CommonText(text: ""));
  }
}
